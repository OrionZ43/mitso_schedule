<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash screen API (Android 12+). Иконки нет: сплэш — только фон, а сразу
         после первого кадра Flutter показывает экран загрузки с анимированным
         M3LoadingIndicator. Фон — нейтральный тон системной палитры Material You,
         тот же, что у фона приложения, поэтому переход не мигает.

         Родитель — NoTitleBar, как в шаблоне Flutter: у DeviceDefault.DayNight
         есть заголовок окна, и он рисовался над приложением полосой с его именем. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@android:color/system_neutral1_900</item>
        <item name="android:windowSplashScreenAnimatedIcon">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
